let firstName; //declaring a varaible without initializing it 

console.log(firstName); //putput is undefined

function doSomthing(){
//This function will not return a value so it will return undifened
}
console.log('Dosomthing function is ', doSomthing());

let noValue=null   //Explicitly indicating that no meaningful value is assigned. 

console.log('The value for a null varaiable is ',noValue);